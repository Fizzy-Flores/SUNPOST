services:
  - type: web
    name: sunpost-backend
    env: python
    pythonVersion: 3.12.0
    plan: free
    buildCommand: cd WEBSITE/Sunpost/backend && pip install -r requirements.txt
    startCommand: cd WEBSITE/Sunpost/backend && uvicorn main:app --host 0.0.0.0 --port $PORT
